#!/bin/bash
set -euo pipefail

exec bwrap --ro-bind / / \
           --bind "$HOME" "$HOME" \
           --dev /dev \
           --unshare-all \
           --die-with-parent \
           -- /usr/local/bin/micro "$@"
